
class SumSolution:
    
    def compute(self, x:int, y:int)->int:
        if not (0<=x<=100):
            raise ValueError(f"")
        if not (0<=y<=100)
        return x+y
        raise NotImplementedError()


